<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="designpattern.projectsInAction.mybatis.UserMapper">
    <select id="selectById" resultType="designpattern.projectsInAction.mybatis.UserDo">
        select * from users where id=#{id}
    </select>
</mapper>
